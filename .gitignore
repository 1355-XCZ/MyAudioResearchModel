# Local configuration (contains secrets)
local_config.sh

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
reproduce_venv/
test_venv/
test_venv_*/
venv_test_*/

# Jupyter Notebook
.ipynb_checkpoints
*.ipynb

# PyTorch
*.pt
*.pth
*.ckpt

# Exceptions: keep best models
!checkpoints/grouped_rvq_best.pt
!checkpoints/entropy_model_best.pt
# traditional_rvq_16L.pt is too large (109MB > 100MB GitHub limit)

# Training outputs
checkpoints/logs/
checkpoints/results/
indices_cache/
logs/
*.log

# Evaluation results (optional: keep or ignore)
evaluation_results/
evaluation_quick_v2_results/
evaluation_results_quick/
evaluation_results_4points/
evaluation_results_test/

# Archive and temporary directories (historical files, not for release)
archive/
backup/
old_code/
checkpoints_backup_normalized/
temp_scripts/
temp_files/
temp_plots/

# data_subset目录：评估子集（提供给评审员，需要提交到Git）
# 已包含随机抽取的1600个样本（seed=42）
# 必须在忽略规则之前声明例外
!data_subset/

# Data files (too large for git)
# 不使用 *.npy 以便data_subset可以被跟踪
# 改为忽略特定目录
data/
!data_subset/
checkpoints/*.npy
checkpoints/**/*.npy
*.npz
*.wav
*.flac
*.mp3

# Exceptions: keep normalization parameters
!ev2_mean_std_100h_EN_ZH.npz
